<h1><%= @trip.id ? 'Edit trip' : 'Create trip' %></h1>
<hr />
<%= form_for @trip do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <%= f.submit %>
<% end %>